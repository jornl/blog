Ruby's syntax is so clean and readable. For example, iterating over an array is super intuitive:

```ruby
fruits = ["apple", "banana", "cherry"]
fruits.each do |fruit|
  puts fruit
end
```

This code will output:

```
apple
banana
cherry
```
